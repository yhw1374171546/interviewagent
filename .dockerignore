# VCS
.git
.github

# 运行时数据（由 compose 卷挂载，不进镜像）
data
logs

# 环境与密钥（绝不进镜像）
.env
.env.*

# Python 缓存
__pycache__
*.py[cod]
.pytest_cache
.ruff_cache

# 覆盖率产物
.coverage
.coverage.*
htmlcov

# 虚拟环境 / 构建产物
.venv
venv
dist
build
*.egg-info

# 文档图片等（可按需保留，默认排除大文件）
docs/images
