services:
  web:
    build: .
    ports:
      - "8000:8000"
    # 从宿主 .env 注入 API Key（首次先 cp .env.example .env）。
    # 未配置 Key 时自动降级 Mock LLM 演示模式，零配置也能体验。
    env_file:
      - .env
    # 会话数据持久化到宿主 ./data，容器重建不丢面试记录
    volumes:
      - ./data:/app/data
    restart: unless-stopped
