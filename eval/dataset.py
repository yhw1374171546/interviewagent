"""
LLM-as-judge 评测数据集
========================
人工标注的评测样本: 从题库选 10 道题，每题 3 个质量档次的回答
（高/中/低），带人工标签分数 — 用于量化评估器的评分准确性。

标注原则:
    - label_score: 人工评定的总分（1-10），作为 ground truth
    - 高/中/低样本的区分维度: 要点覆盖度 + 深度 + 结构
    - 回答文本模拟真实面试者的作答风格（不完美但真实）

用法:
    from eval.dataset import EVAL_SAMPLES
"""


# 每个样本: (question_id, quality, label_score, answer)
# question_id 对应 question_bank.QUESTION_BANK 中的题
EVAL_SAMPLES = [
    # ═══ PY001 GIL（Python 基础，难度 3）═══
    ("PY001", "high", 8.5,
     "GIL 是 CPython 的全局解释器锁，同一时刻只允许一个线程执行字节码，这是为了"
     "保护引用计数等解释器内部状态不被并发破坏。它主要影响 CPU 密集型的多线程程序"
     "——因为线程无法真正并行。绕过方案有三类：CPU 密集用 multiprocessing 多进程，"
     "IO 密集用 asyncio 协程，性能敏感的部分用 C 扩展并在内部释放 GIL。我们项目里"
     "Celery 就是通过多进程 worker 规避 GIL 的。"),
    ("PY001", "mid", 5.5,
     "GIL 就是全局解释器锁，Python 同一时间只有一个线程在跑，所以多线程在 Python"
     "里没什么用。解决办法是用多进程，multiprocessing 可以绕过 GIL。"),
    ("PY001", "low", 2.5,
     "装饰器是 Python 的高级特性，本质是闭包，用 @ 语法糖来包装函数，我在项目里"
     "用装饰器做过日志记录和权限校验。"),

    # ═══ DB001 MySQL 索引（数据库，难度 3）═══
    ("DB001", "high", 9.0,
     "MySQL InnoDB 索引用 B+ 树。原因有三个：一是 B+ 树非叶子节点不存数据，"
     "同样大小的页能存更多索引项，树更矮，磁盘 IO 次数少；二是叶子节点用双向"
     "链表串联，天然支持范围查询和顺序遍历；三是相比红黑树，B+ 树是多叉结构，"
     "更适合磁盘的按页读取。Hash 索引虽然等值查询 O(1)，但不支持范围查询。"),
    ("DB001", "mid", 5.0,
     "MySQL 索引用 B+ 树，因为 B+ 树层级低，查询快，能减少磁盘 IO。"),
    ("DB001", "low", 2.0,
     "MySQL 索引用哈希表实现，等值查询很快，时间复杂度 O(1)，适合所有查询场景。"),

    # ═══ AI001 RAG 检索增强（LLM，难度 3）═══
    ("AI001", "high", 8.5,
     "RAG 的流程是：先把文档清洗成纯文本，按语义边界切成 chunk；然后用 Embedding"
     "模型把每个 chunk 转成向量存进向量库。查询时把用户问题也向量化，在向量库里"
     "做相似度检索取 Top-K，把检索结果和问题拼成 prompt 给 LLM 生成答案。chunk "
     "大小要在语义完整性和检索精度之间平衡，一般 200-500 token；不能整篇喂给 "
     "LLM 是因为上下文窗口有限、成本高、且无关内容会稀释注意力。"),
    ("AI001", "mid", 5.0,
     "RAG 就是检索增强生成，把文档转成向量存起来，查询的时候找相似的文档片段，"
     "和问题一起给大模型，让它基于资料回答。"),
    ("AI001", "low", 2.0,
     "Transformer 的 self-attention 机制通过 QKV 三个矩阵计算注意力权重，"
     "多头注意力可以让模型关注不同的表示子空间，位置编码用来补充序列信息。"),

    # ═══ GO001 GMP 调度（Go，难度 4）═══
    ("GO001", "high", 8.0,
     "GMP 里 G 是 goroutine，M 是操作系统线程，P 是逻辑处理器（默认等于核数）。"
     "P 维护本地运行队列，调度器把 G 放进 P 的队列，M 从 P 拿 G 执行。当 G 做"
     "系统调用阻塞时，M 会和 P 解绑，调度器唤醒或新建一个 M 接手 P 继续跑其他"
     "G；如果本地队列空了就从别的 P 偷任务（work stealing），所以 Go 的并发"
     "开销比线程低得多。"),
    ("GO001", "mid", 5.0,
     "G 是 goroutine，M 是线程，P 是处理器。goroutine 比线程轻量，Go 运行时"
     "负责调度它们，阻塞的时候会切换到别的 goroutine。"),
    ("GO001", "low", 2.5,
     "Go 用 channel 做并发通信，channel 底层是 hchan 结构，有环形队列。"
     "无缓冲 channel 需要发送和接收同时就绪，有缓冲的在满的时候才阻塞。"),

    # ═══ NET003 TCP 握手挥手（网络，难度 3）═══
    ("NET003", "high", 8.5,
     "三次握手：客户端发 SYN，服务端回 SYN+ACK，客户端再发 ACK，双方确认彼此的"
     "收发能力。握手要三次是因为要防止历史连接请求导致服务端单方面建立无效连接。"
     "挥手要四次是因为 TCP 是全双工的，双方都要单独发 FIN 和 ACK，没有合并的"
     "场景。TIME_WAIT 是主动关闭方等待 2MSL，确保最后的 ACK 能重传、旧连接的"
     "报文在网络中消失，避免串扰新连接。"),
    ("NET003", "mid", 5.0,
     "三次握手就是 SYN、SYN+ACK、ACK 三个包，建立连接。四次挥手是双方各发 "
     "FIN 和 ACK，关闭连接。TIME_WAIT 是为了保证连接可靠关闭。"),
    ("NET003", "low", 2.0,
     "TCP 是可靠的传输协议，HTTP 跑在 TCP 之上。浏览器输入 URL 之后会先做 "
     "DNS 解析，然后建立连接、发送请求、渲染页面。"),

    # ═══ SD001 短链接系统（系统设计，难度 4）═══
    ("SD001", "high", 9.0,
     "短链接系统的核心是长短链映射。设计：发号器用 Snowflake 生成唯一 ID，"
     "再转 62 进制得到短码，保证不重复且有序；存储用 MySQL 分库分表，短码做"
     "主键；热点数据加 Redis 缓存，缓存未命中才查库；跳转用 302 重定向以便"
     "统计点击。量级：日生成百万级、访问千万级，读多写少，QPS 峰值约几百，"
     "单机 + 缓存就能扛住，重点是 ID 生成不重复和缓存命中率。"),
    ("SD001", "mid", 5.5,
     "短链接就是用一个短的字符串替代长 URL，访问时重定向回原地址。实现就是"
     "数据库存一个映射表，短码是随机生成的，用缓存加速查询。"),
    ("SD001", "low", 2.5,
     "短链接系统可以用 Redis 的 sorted set 实现，ZADD 更新分数，ZREVRANGE "
     "获取排名，用分布式锁保证一致性。"),

    # ═══ JV004 synchronized 锁升级（Java，难度 4）═══
    ("JV004", "high", 8.5,
     "synchronized 锁升级过程：无锁 → 偏向锁 → 轻量级锁 → 重量级锁。偏向锁"
     "假设只有一个线程会反复获取锁，在对象头记录线程 ID，同线程重入无需 CAS；"
     "出现竞争时升级轻量级锁，通过 CAS 自旋尝试获取，适合持有时间短的场景；"
     "自旋失败次数过多升级重量级锁，线程挂起进入等待队列，由操作系统调度。"
     "这是 JVM 对'大多数锁竞争不激烈'的乐观优化，锁消除和锁粗化是编译期的"
     "配套优化。"),
    ("JV004", "mid", 5.0,
     "synchronized 会从偏向锁升级到轻量级锁再升级到重量级锁。偏向锁记录线程 "
     "ID，轻量级锁用 CAS 自旋，重量级锁要操作系统介入，性能差。"),
    ("JV004", "low", 2.0,
     "HashMap 在 JDK 1.8 里是数组加链表加红黑树，链表长度超过 8 就转红黑树，"
     "扩容的时候 rehash。"),

    # ═══ FE003 Vue 响应式（前端，难度 4）═══
    ("FE003", "high", 8.5,
     "Vue2 用 Object.defineProperty 劫持对象的 getter/setter，读取时收集依赖，"
     "修改时通知 watcher 更新。缺陷是新增属性检测不到（要用 $set）、数组下标"
     "修改无法触发。Vue3 改用 Proxy，可以拦截包括新增/删除/数组操作在内的全部"
     "属性变更，性能也更好（懒代理，不用初始化时递归遍历）。依赖收集的本质是"
     "发布订阅模式，组件 render 时收集，数据变更时触发。"),
    ("FE003", "mid", 5.0,
     "Vue 的响应式就是数据变化自动更新视图。Vue2 用 defineProperty，Vue3 用 "
     "Proxy，Proxy 更强大，能监听数组和新增属性。"),
    ("FE003", "low", 2.0,
     "React 的虚拟 DOM 通过 diff 算法对比新旧树，只更新变化的部分。列表渲染"
     "需要 key 来帮助 diff 识别元素身份。"),

    # ═══ MQ003 消息不丢失（消息队列，难度 4）═══
    ("MQ003", "high", 8.5,
     "消息不丢失要保证三端：生产端开启确认机制（Kafka acks=all 或 RabbitMQ "
     "confirm），失败重试直到写入成功；Broker 端做持久化（Kafka 副本落盘、"
     "RabbitMQ 持久化队列），多副本保障单点故障不丢数据；消费端关闭自动提交，"
     "处理完业务后再手动提交 offset，失败时重试或进死信队列兜底。三端配合才能"
     "做到 at-least-once，配合幂等设计达到业务上的 exactly-once 效果。"),
    ("MQ003", "mid", 5.0,
     "消息不丢失主要是消费端处理完再提交 offset，Broker 做持久化，生产端开启"
     "确认机制，这样三端都能保证消息可靠。"),
    ("MQ003", "low", 2.0,
     "消息队列可以实现削峰填谷，把突发流量缓冲下来让消费者慢慢处理，我们项目"
     "用 Kafka 做过订单消息的异步处理。"),

    # ═══ DB002 事务隔离级别（数据库，难度 4）═══
    ("DB002", "high", 8.5,
     "四个隔离级别：读未提交（脏读）、读已提交（不可重复读）、可重复读（幻读）、"
     "串行化。脏读是读到了别的事务未提交的数据；不可重复读是同一事务两次读同一行"
     "结果不同；幻读是两次范围查询的结果集行数不同。InnoDB 在 RR 级别下用 MVCC "
     "快照读解决不可重复读和大部分幻读，配合间隙锁（next-key lock）解决当前读的"
     "幻读，所以 InnoDB 的 RR 实际接近串行化的保护能力。"),
    ("DB002", "mid", 5.0,
     "隔离级别有四种，从低到高是读未提交、读已提交、可重复读、串行化。级别越高"
     "一致性越好但并发越低。MySQL 默认是可重复读，用 MVCC 实现。"),
    ("DB002", "low", 2.0,
     "数据库的索引要建在区分度高的列上，联合索引要遵循最左前缀原则，不然索引"
     "会失效，查询会全表扫描。"),
]
